const pathToIcons = '../assets/icons'
const pathToImages = '../assets/images'
const pathToVideos = '../assets/videos'

const Icons = {
    homeIcon:require(`${pathToIcons}/logo.jpg`),
    doneIcon:require(`${pathToIcons}/doneIcon.png`),

}

const Images = {
    doneImage:require(`${pathToImages}/done.png`),
  

}

const Videos = {
}

export {Icons,Images,Videos};